#pragma once

vec2 GetScreenTextureCoords(uint vertexIndex)
{
	return vec2((vertexIndex << 1) & 2, vertexIndex & 2);
}

vec4 TextureCoordsToClipSpace(vec2 texCoords)
{
	return vec4(texCoords * 2.f - 1.f, 0.f, 1.f);
}
